describe("TestProfile", () => {
    it("Navigate to homepage", function() {
        // Navigate to homepage
        cy.navigateToHome();

    });
    it("Navigate to profile page", function() {
        //TODO Navigate to localhost:8081 as auth0 callback
        //     cy.visit("/profile");
        });

    it("have user image", function() {
        // cy.get()
    })
});
